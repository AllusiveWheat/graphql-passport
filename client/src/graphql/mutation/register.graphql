mutation Register(
  $password: String!
  $email: String!
  $lastName: String!
  $firstName: String!
) {
  register(
    password: $password
    email: $email
    lastName: $lastName
    firstName: $firstName
  ) {
    id
    firstName
    lastName
    email
    password
  }
}
